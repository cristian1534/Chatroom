{
  "name": "chat",
  "version": "1.0.0",
  "description": "Chat server for E-Learning.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "dev": "nodemon server.js",
    "dev:css": "concurrently \"npm run dev\" \"npm run css\""
  },
  "author": "Cristian Machuca",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "express": "^4.18.2",
    "socket.io": "^4.6.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22",
    "tailwindcss": "^3.3.2"
  }
}
